# ============================================================
# Volqan Environment Configuration
# Generated by create-volqan-app for: {{PROJECT_NAME}}
# ============================================================

# Node.js environment
NODE_ENV=development

# ---------------------------------------------------------------------------
# Database
# ---------------------------------------------------------------------------
# {{DB_PROVIDER}} connection string
DATABASE_URL="{{DATABASE_URL}}"

# ---------------------------------------------------------------------------
# Authentication
# ---------------------------------------------------------------------------
# JWT secret (auto-generated — keep secret, rotate regularly)
JWT_SECRET="{{JWT_SECRET}}"

# ---------------------------------------------------------------------------
# Server
# ---------------------------------------------------------------------------
PORT=3000
ADMIN_PORT=3001

# ---------------------------------------------------------------------------
# Auth Providers (fill in if enabled)
# ---------------------------------------------------------------------------
{{#if AUTH_GOOGLE}}
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_REDIRECT_URI="http://localhost:3000/api/auth/google/callback"
{{/if}}
{{#if AUTH_GITHUB}}
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""
GITHUB_REDIRECT_URI="http://localhost:3000/api/auth/github/callback"
{{/if}}

# ---------------------------------------------------------------------------
# Optional: Storage (AWS S3 / Google Cloud Storage)
# ---------------------------------------------------------------------------
# AWS_ACCESS_KEY_ID=""
# AWS_SECRET_ACCESS_KEY=""
# AWS_REGION="us-east-1"
# S3_BUCKET=""

# ---------------------------------------------------------------------------
# Optional: Bazarix Marketplace License
# For paid extensions and themes: https://bazarix.link
# ---------------------------------------------------------------------------
# BAZARIX_LICENSE_KEY=""
